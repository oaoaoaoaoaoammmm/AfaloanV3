spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  cloud:
    config:
      enabled: false
    openfeign:
      afa-order:
        name: ${AFA_ORDER_NAME:afa-order}

eureka:
  client:
    enabled: false

api:
  job:
    scheduler:
      process-debt: "-"
